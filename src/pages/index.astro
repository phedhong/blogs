---
import Layout from '@layouts/Layout.astro'
import App from '@components/forms/App.svelte'
import Todos from '@components/forms/Todos.svelte'

let models = [
	{ id: 1, title: 'signin', fields: { name: 'name', password: 'password', username: 'username' } }
]

const collections = 'Home'
---

<Layout title="Home">
	<App client:only="svelte" />
</Layout>
